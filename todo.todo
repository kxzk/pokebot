TODO

- [ ] screenshot -> llm pipeline (validate game understanding)
  - [ ] take screenshot with adb
  - [ ] take image send to llm
- [ ] collect more ui locations
- [ ] figure out naming scheme for ui
- [ ] how to extract battle log

NOTE

* llm might get costly, swap to ollama
* parse limitless tcg deck fmt -> decklist
* exp farmer bot (using system auto battle feature)

DONE

- [x] adb command spacing is off (there is none)
- [x] add enum for UI elements buttons
- [x] simplify python data pull scripts (only pull if new data, incremental)
